---
- name: Simple Ansible Playbook
  hosts: all
  become: true
  gather_facts: true

  vars:
    my_variable: "Hello, World!"

  tasks:
    - name: Display the variable
      debug:
        msg: "{{ my_variable }}"
